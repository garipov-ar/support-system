# Руководство Администратора
## Система Технической Поддержки (Telegram Bot + Django)

---

## 1. Требуемые Ресурсы

### 1.1 Аппаратные Требования

**Минимальные требования:**
- CPU: 2 ядра
- RAM: 2 GB
- Диск: 10 GB свободного места
- Сеть: Стабильное подключение к интернету

**Рекомендуемые требования:**
- CPU: 4 ядра
- RAM: 4 GB
- Диск: 20 GB SSD
- Сеть: Выделенный канал с минимальной задержкой

### 1.2 Программные Требования

- **Операционная система**: Linux (Ubuntu 20.04+), Windows 10/11, macOS 10.15+
- **Docker**: версия 20.10+
- **Docker Compose**: версия 2.0+
- **Telegram Bot Token**: получить у [@BotFather](https://t.me/BotFather)

### 1.3 Сетевые Требования

- Открытый доступ к Telegram API (`api.telegram.org`)
- Порт `8000` для веб-интерфейса (можно изменить)
- Порт `5432` для PostgreSQL (внутренний, не требует внешнего доступа)

---

## 2. Установка и Настройка

### 2.1 Первоначальная Установка

#### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd support-system
```

#### Шаг 2: Создание файла конфигурации

Создайте файл `.env` в корне проекта:

```env
# Django
SECRET_KEY=your_random_secret_key_here_change_this
DEBUG=0
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Database
POSTGRES_DB=support
POSTGRES_USER=support
POSTGRES_PASSWORD=strong_password_here
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Telegram
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

> [!IMPORTANT]
> **Безопасность**: Обязательно измените `SECRET_KEY` и `POSTGRES_PASSWORD` на уникальные значения!

#### Шаг 3: Запуск системы

```bash
# Сборка и запуск всех контейнеров
docker compose up -d --build
```

Система запустит следующие сервисы:
- `db` - PostgreSQL база данных
- `redis` - Redis для кэша и персистентности бота
- `web` - Django веб-сервер (порт 8000)
- `bot` - Telegram бот
- `worker` - Celery worker для фоновых задач
- `beat` - Celery beat для периодических задач
- `monitor` - Сторожевой таймер для мониторинга бота

#### Шаг 4: Инициализация базы данных

```bash
# Применить миграции
docker compose exec web python manage.py migrate

# Создать суперпользователя
docker compose exec web python manage.py createsuperuser

# Настроить роли (Content Manager)
docker compose exec web python manage.py setup_roles
```

#### Шаг 5: Проверка работоспособности

1. Откройте браузер: `http://localhost:8000/admin/`
2. Войдите с учетными данными суперпользователя
3. Проверьте раздел **Bot → Статус бота** (должен быть 🟢 Работает)
4. Напишите боту в Telegram команду `/start`

### 2.2 Настройка Уведомлений

Для получения уведомлений о сбоях и ошибках:

1. Войдите в админ-панель
2. Перейдите в **Bot → Настройки уведомлений администраторов**
3. Нажмите **Добавить**
4. Выберите пользователя и укажите Telegram ID
5. Отметьте типы событий для уведомлений
6. Сохраните

> [!TIP]
> Узнать свой Telegram ID можно у бота [@userinfobot](https://t.me/userinfobot)

### 2.3 Настройка Контента

#### Создание Категорий

1. Перейдите в **Content → Дерево контента**
2. Нажмите **Добавить узел контента**
3. Заполните поля:
   - **Title**: Название категории
   - **Parent**: Родительская категория (оставьте пустым для корневой)
   - **Order**: Порядок сортировки
   - **Visible in bot**: Видимость в боте
   - **Это папка**: Отметьте для категории-папки

#### Создание Документов

Документы теперь являются узлами дерева с `is_folder=False`:

1. Перейдите в **Content → Дерево контента**
2. Нажмите **Добавить узел контента**
3. Заполните поля:
   - **Title**: Название документа
   - **Parent**: Родительская категория
   - **Это папка**: Снимите галочку
   - **Equipment**: Связанное оборудование (опционально)
   - **Description**: Описание документа

#### Загрузка Файлов

1. Перейдите в **Content → Версии файлов**
2. Нажмите **Добавить версию файла**
3. Выберите узел контента (документ)
4. Укажите версию и загрузите файл
5. Сохраните

---

## 3. Управление Пользователями

### 3.1 Роли и Права Доступа

Система использует RBAC (Role-Based Access Control):

| Роль | Права |
|------|-------|
| **Administrator** | Полный доступ ко всем разделам |
| **Content Manager** | Управление контентом и просмотр аналитики |
| **Staff** | Базовый доступ к админ-панели |

### 3.2 Создание Пользователей

1. Перейдите в **Users → Пользователи**
2. Нажмите **Добавить пользователя**
3. Заполните данные и назначьте группу
4. Сохраните

> [!WARNING]
> Обычные пользователи не могут редактировать или удалять суперпользователей!

---

## 4. Мониторинг и Аналитика

### 4.1 Дашборд Аналитики

Доступ: **Analytics → Bot interactions → Открыть Дашборд**

Метрики:
- 📊 Активность пользователей по дням
- ⏱️ Среднее время ответа бота
- 🔍 Топ-10 поисковых запросов
- 👥 Рейтинг активных пользователей

### 4.2 Журнал Аудита

Доступ: **Analytics → Audit log**

Фиксирует:
- Вход администраторов
- Изменения документов и категорий
- Системные события

### 4.3 Статус Системы

Проверка: **Bot → Статус бота**

Индикаторы:
- 🟢 **Работает**: Бот активен
- 🔴 **Не работает**: Требуется вмешательство

---

## 5. Обслуживание

### 5.1 Резервное Копирование

#### База данных

```bash
# Создать бэкап
docker compose exec db pg_dump -U support support > backup_$(date +%Y%m%d).sql

# Восстановить из бэкапа
docker compose exec -T db psql -U support support < backup_20260113.sql
```

#### Медиа-файлы

```bash
# Создать архив
tar -czf media_backup_$(date +%Y%m%d).tar.gz backend/media/

# Восстановить
tar -xzf media_backup_20260113.tar.gz
```

### 5.2 Обновление Системы

```bash
# Остановить контейнеры
docker compose down

# Получить обновления
git pull

# Пересобрать и запустить
docker compose up -d --build

# Применить миграции
docker compose exec web python manage.py migrate
```

### 5.3 Просмотр Логов

```bash
# Все сервисы
docker compose logs -f

# Конкретный сервис
docker compose logs -f bot
docker compose logs -f web
docker compose logs -f monitor
```

---

## 6. Аварийные Ситуации

### 6.1 Бот не отвечает

**Симптомы**: Статус бота 🔴, пользователи не получают ответы

**Решение**:

```bash
# Проверить логи
docker compose logs bot --tail=100

# Перезапустить бот
docker compose restart bot

# Если не помогло - пересоздать контейнер
docker compose up -d --force-recreate bot
```

### 6.2 Ошибки базы данных

**Симптомы**: Ошибки при сохранении данных, 500 ошибки

**Решение**:

```bash
# Проверить состояние БД
docker compose exec db psql -U support -c "SELECT version();"

# Перезапустить БД
docker compose restart db

# Проверить миграции
docker compose exec web python manage.py showmigrations
```

### 6.3 Переполнение диска

**Симптомы**: Система не сохраняет файлы

**Решение**:

```bash
# Проверить использование диска
df -h

# Очистить старые Docker образы
docker system prune -a

# Удалить старые логи
docker compose logs --tail=0 > /dev/null
```

### 6.4 Проблемы с Redis

**Симптомы**: Бот "забывает" состояния пользователей

**Решение**:

```bash
# Перезапустить Redis
docker compose restart redis

# Проверить подключение
docker compose exec redis redis-cli ping
```

---

## 7. Удаление Системы

### 7.1 Полное удаление

```bash
# Остановить и удалить контейнеры
docker compose down

# Удалить volumes (БД и Redis данные)
docker compose down -v

# Удалить образы
docker rmi $(docker images -q support-system*)
```

### 7.2 Сохранение данных перед удалением

```bash
# Создать бэкапы
docker compose exec db pg_dump -U support support > final_backup.sql
tar -czf media_final.tar.gz backend/media/

# Затем удалить систему
docker compose down -v
```

---

## 8. Рекомендации по Безопасности

- ✅ Используйте сильные пароли для БД и Django
- ✅ Отключите DEBUG в продакшене (`DEBUG=0`)
- ✅ Настройте HTTPS для веб-интерфейса
- ✅ Регулярно обновляйте зависимости
- ✅ Ограничьте доступ к порту 8000 через firewall
- ✅ Регулярно создавайте резервные копии
- ✅ Мониторьте журнал аудита на подозрительную активность

---

## 9. Контакты и Поддержка

При возникновении проблем:
1. Проверьте логи: `docker compose logs -f`
2. Изучите раздел "Аварийные ситуации"
3. Обратитесь к разработчику системы
