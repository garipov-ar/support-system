# Система Технической Поддержки (Telegram Bot + Django)

Проект представляет собой систему для управления и дистрибуции технической документации через Telegram-бота. Администраторы управляют контентом через веб-интерфейс, а пользователи получают доступ к файлам и инструкциям через бота.

## 🚀 Возможности

### Веб-панель (Админка)
-   **Категории**: Создание иерархической структуры (дерево категорий с неограниченной вложенностью).
-   **Документы**: Загрузка файлов любых форматов с версионированием.
-   **Описания**: Добавление текстовых описаний к файлам с поддержкой CKEditor 5.
-   **Аналитика**: Дашборд с графиками активности, времени ответа, топ-запросов и активных пользователей.
-   **Управление пользователями**: Просмотр зарегистрированных пользователей бота и их подписок.

### Telegram Бот
-   **Навигация**: 
    -   Удобное меню по категориям и подкатегориям.
    -   Визуальные "хлебные крошки" для понимания текущего местоположения.
    -   Различные иконки для корневых категорий (🗂) и подкатегорий (📂).
-   **Подписки**:
    -   Подписка на обновления категорий с наследованием на подкатегории.
    -   Визуальная индикация статуса подписки (🔔/🔕).
-   **Поиск**:
    -   Интерактивный поиск по названию и описанию документов.
    -   Кнопка "🔍 Поиск" в главном меню и каждой категории.
    -   Результаты отображаются как список кнопок для выбора нужного документа.
-   **Умная выдача**:
    -   Изображения (`jpg`, `png` и др.) приходят как **фото**.
    -   Остальные файлы приходят как **документы**.
    -   Описание файла отображается в подписи (caption).
-   **UX**: 
    -   Меню навигации автоматически восстанавливается после скачивания файла.
    -   Единообразный интерфейс во всех режимах работы.
-   **Регистрация**: Сбор имени, email и согласия на обработку данных при первом запуске.
-   **Производительность**: Асинхронная работа (httpx) для быстрого обслуживания множества пользователей.

### Аналитика и Мониторинг
-   **Сбор данных**: Логирование всех взаимодействий пользователей с ботом.
-   **Визуализация**: Интерактивный дашборд в Django Admin с графиками Chart.js.
-   **Метрики**:
    -   Активность пользователей по дням.
    -   Среднее время ответа бота.
    -   Топ-10 поисковых запросов (для выявления пробелов в документации).
    -   Рейтинг самых активных пользователей.
-   **Доступ**: Только для администраторов и авторизованного персонала.

## 🛠 Технологический стек

-   **Backend**: Python 3.11, Django 5, Django REST Framework.
-   **Database**: PostgreSQL с django-mptt для иерархических структур.
-   **Bot**: python-telegram-bot, httpx (Async HTTP Client).
-   **Frontend**: CKEditor 5 для редактирования описаний, Chart.js для аналитики.
-   **Infra**: Docker, Docker Compose.

## 📦 Установка и запуск

### 1. Предварительные требования
-   Docker и Docker Compose
-   Токен Telegram бота (получить у @BotFather)

### 2. Настройка окружения
Создайте файл `.env` в корне проекта:

```env
# Django
SECRET_KEY=your_secret_key_here
DEBUG=1
ALLOWED_HOSTS=*

# Database
POSTGRES_DB=support
POSTGRES_USER=support
POSTGRES_PASSWORD=support
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Telegram
TELEGRAM_BOT_TOKEN=ваш_токен_бота
```

### 3. Запуск
Соберите и запустите контейнеры:

```bash
docker-compose up -d --build
```

### 4. Инициализация
При первом запуске примените миграции и создайте администратора:

```bash
# Применить миграции
docker-compose exec web python manage.py migrate

# Создать суперпользователя для входа в админку
docker-compose exec web python manage.py createsuperuser
```

Админка будет доступна по адресу: `http://localhost:8000/admin/`

## 📂 Структура проекта

-   `backend/` - Исходный код Django проекта.
    -   `apps/content` - Управление документами и категориями.
    -   `apps/bot` - Логика Telegram бота и модели пользователей.
    -   `apps/analytics` - Система сбора и визуализации аналитики.
-   `docker-compose.yml` - Описание сервисов (db, web, bot).

## 🔧 Особенности реализации

-   **Общие медиа-файлы**: Используется bind mount (`./backend/media:/app/media`), чтобы веб-сервер и бот имели доступ к одним и тем же файлам.
-   **Логирование**: Включен режим `PYTHONUNBUFFERED=1` для просмотра логов в реальном времени.
-   **Версионирование документов**: Каждый документ может иметь несколько версий, бот всегда выдает последнюю.
-   **Иерархия категорий**: Использование django-mptt для эффективной работы с деревом категорий.
-   **Аналитика**: Все действия пользователей логируются с временными метками и временем ответа.

## 📝 Разработка

### Обновление кода бота
Для применения изменений в коде бота требуется перезапуск контейнера:

```bash
docker-compose restart bot
```

### Обновление веб-сервера
Django автоматически перезагружается при изменении кода в режиме DEBUG.

### Просмотр логов
```bash
# Все сервисы
docker-compose logs -f

# Только бот
docker-compose logs -f bot

# Только веб
docker-compose logs -f web
```

## 📊 Использование аналитики

1. Войдите в админку как суперпользователь
2. Перейдите в раздел "Аналитика" → "Bot interactions"
3. Нажмите кнопку "Открыть Дашборд" в правом верхнем углу
4. Изучайте графики активности, производительности и популярных запросов

## 🤝 Вклад в проект

Проект создан для внутреннего использования, но открыт для улучшений и предложений.

## 📄 Лицензия

MIT License